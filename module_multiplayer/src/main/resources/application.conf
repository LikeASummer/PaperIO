akka.grpc.client {
    "com.paper.io.LobbyService" {
        host = 127.0.0.1
        port = 8080
        override-authority = foo.test.google.fr
        trusted = /ca.pem
    }
    "com.paper.io.SessionService" {
        host = 127.0.0.1
        port = 8080
        override-authority = foo.test.google.fr
        trusted = /ca.pem
    }
}